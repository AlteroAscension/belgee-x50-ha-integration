# Implementation placeholder

The integration is intentionally documentation-first. Runtime files will be
added during Roadmap Phase 1; this directory is reserved so the final package
layout and domain (`belgee_x50`) cannot drift.

Do not add a fake `manifest.json` or install this directory before the first
working Config Flow, coordinator and tests exist.

